import { TourFilterPipe } from './tour-filter.pipe';

describe('TourFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TourFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
